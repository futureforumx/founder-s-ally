// ---------------------------------------------------------------------------
// CirclesPage — placeholder shell for investor circles (side nav entry).
// ---------------------------------------------------------------------------

export function CirclesPage() {
  return (
    <div className="space-y-1">
      <h1 className="text-xl font-semibold tracking-tight text-foreground">Circles</h1>
      <p className="text-xs text-muted-foreground">Define and manage investor circles for this context.</p>
    </div>
  );
}
