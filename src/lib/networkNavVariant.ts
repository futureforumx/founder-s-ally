/**
 * Experimental label for the community / directory surface (`activeView === "network"`, etc.).
 * View keys and routes stay `network` — change this string only to try a new IA without refactors.
 */
export const NETWORK_SURFACE_DISPLAY_NAME = "VARIANT";

/** Uppercase sidebar section heading (matches prior "NETWORK" styling). */
export const NETWORK_SURFACE_SECTION_HEADING = NETWORK_SURFACE_DISPLAY_NAME.toUpperCase();
