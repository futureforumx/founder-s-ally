/** Shown when a workspace member tries connect / disconnect / resync (owners only for workspace contexts). */
export const CONNECTOR_MANAGE_DENIED_MESSAGE =
  "Only workspace owners can connect, disconnect, or re-sync integrations for this context.";
