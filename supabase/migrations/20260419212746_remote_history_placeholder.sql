-- Placeholder: version exists in remote `supabase_migrations.schema_migrations` but had no local file.
-- Keeps `supabase db push` validation in sync. Safe no-op if ever applied on an empty DB.
SELECT 1;
